# Redis configuration for FreeCalls

# Persistence
save 60 1000
appendonly yes
appendfsync everysec

# Memory
maxmemory 2gb
maxmemory-policy volatile-lru

# Networking
bind 0.0.0.0
port 6379
timeout 0
tcp-keepalive 300
